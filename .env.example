HOSTNAME=localhost
APP_ENV=development

NGINX_PORT=8080
NGINX_HOST=${HOSTNAME}:${NGINX_PORT}

CLIENT_PORT=3000
CLIENT_HOST=${HOSTNAME}:${CLIENT_PORT}

SERVER_PORT=3001
SERVER_HOST=${HOSTNAME}:${SERVER_PORT}

IMAGES_PORT=3002
IMAGES_HOST=${HOSTNAME}:${IMAGES_PORT}

INJECTOR_PORT=3003

DB_PORT=5432

# Server settings
TOKEN_SECRET=token-secret

# Database Settings
DB_USER=zeradraws
DB_PASS=password
DB_NAME=dummy

# Manual Server config
DB_HOST=localhost
IMAGES_DIR=.volumes/images
IMAGES_URL=https://${IMAGES_HOST}

# Environment config
RAILS_ENV=${APP_ENV}
NODE_ENV=${APP_ENV}
REACT_APP_ENV=${APP_ENV}

# Seed Data
ADMIN_USER=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASS=iamadmin
